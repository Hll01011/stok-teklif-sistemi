-- BASELINE MIGRATION
-- The live Supabase project already contains the application schema.
-- This legacy file must remain in the repository for migration ordering,
-- but must not recreate or alter the live schema during Supabase Preview.
-- Real schema changes are tracked by the timestamped migrations below.

select 1;